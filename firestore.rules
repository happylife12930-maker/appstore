
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // فتح الوصول الكامل لجميع المستندات لضمان تخطي مشكلة الصلاحيات أثناء الإعداد
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
