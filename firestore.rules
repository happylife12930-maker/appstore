
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // تم فتح القواعد بالكامل لضمان الدخول وحل مشكلة الصلاحيات أثناء التطوير
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
