
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // فتح القواعد مؤقتاً لضمان وصول المدير وإعداد الحسابات
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
