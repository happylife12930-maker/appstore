
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // فتح القواعد بالكامل لضمان عدم حدوث أي خطأ في الصلاحيات أثناء التطوير
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
