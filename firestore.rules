rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // --- Helper Functions ---
    function isAuthenticated() {
      return request.auth != null;
    }

    function getUserData() {
      return get(/databases/$(database)/documents/users/$(request.auth.uid)).data;
    }

    function isAdmin() {
      return isAuthenticated() && getUserData().role == 'admin';
    }

    // --- Security Rules ---

    match /users/{userId} {
      allow get, update, create: if isAuthenticated() && request.auth.uid == userId;
      allow list: if isAdmin();
      allow delete: if false;
    }

    match /users_provision/{email} {
      // السماح بالتحقق والحذف للمستفيد صاحب البريد
      allow get: if true;
      allow delete: if isAuthenticated() && request.auth.token.email.lower() == email.lower();
      allow list, write: if isAdmin();
    }

    match /clients/{clientId} {
      allow get, list: if isAdmin() || (isAuthenticated() && getUserData().clientId == clientId);
      allow write: if isAdmin();
    }

    match /projects/{projectId} {
      // الربط الصارم بين المستفيد ومشاريعه عبر clientId
      allow read, list: if isAdmin() || (isAuthenticated() && resource.data.clientId == getUserData().clientId);
      allow write: if isAdmin();
    }

    match /invoices/{invoiceId} {
      allow read, list: if isAdmin() || (isAuthenticated() && resource.data.clientId == getUserData().clientId);
      allow write: if isAdmin();
    }
    
    match /payments/{paymentId} {
      allow read, list: if isAdmin() || (isAuthenticated() && resource.data.clientId == getUserData().clientId);
      allow write: if isAdmin();
    }
  }
}