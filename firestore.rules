rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // قواعد حماية مفتوحة بالكامل بناءً على طلب المستخدم لتسهيل التطوير
      allow read, write: if true;
    }
  }
}
