rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // السماح بالوصول الكامل لجميع العمليات حالياً لحل مشاكل التهيئة الأولى
    match /{document=**} {
      allow read, write: if true;
    }

  }
}